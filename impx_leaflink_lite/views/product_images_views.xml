<odoo>
    <record id="product_image_kanban_template_view" model="ir.ui.view">
        <field name="name">product.image.kanban.template.view</field>
        <field name="model">product.images</field>
        <field name="arch" type="xml">
            <kanban string="Product Images" default_order="position">
                <field name="id"/>
                <field name="name"/>
                <field name="image"/>
                <field name="position" widget="handle"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="card oe_kanban_global_click p-0">
                            <div class="o_squared_image">
                                <img class="card-img-top"
                                     t-att-src="kanban_image('product.images', 'image', record.id.value)"
                                     t-att-alt="record.name.value"/>
                            </div>
                            <div class="card-body p-0">
                                <h4 class="card-title p-2 m-0 bg-200">
                                    <small>
                                        <field name="name"/>
                                    </small>
                                </h4>
                            </div>
                            <!-- below 100 Kb: good -->
                            <t t-if="record.image.raw_value.length &lt; 100*1000">
                                <t t-set="size_status" t-value="'badge-success'"/>
                                <t t-set="message">Acceptable file size</t>
                            </t>
                            <!-- below 1000 Kb: decent -->
                            <t t-elif="record.image.raw_value.length &lt; 1000*1000">
                                <t t-set="size_status" t-value="'badge-warning'"/>
                                <t t-set="message">Huge file size. The image should be optimized/reduced.</t>
                            </t>
                            <!-- above 1000 Kb: bad -->
                            <t t-else="1">
                                <t t-set="size_status" t-value="'badge-danger'"/>
                                <t t-set="message">Optimization required! Reduce the image size or increase your
                                    compression settings.
                                </t>
                            </t>
                            <span t-attf-class="badge #{size_status} o_product_image_size"
                                  t-esc="record.image.value" t-att-title="message"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="product_images_form_view" model="ir.ui.view">
        <field name="name">product.images.form.view</field>
        <field name="model">product.images</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="position"/>
                            <field name="product_id"/>
                        </group>
                        <group>
                            <field name="image" widget="image"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>